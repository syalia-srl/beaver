[pytest]
testpaths = tests
asyncio_mode = auto
timeout = 30
markers =
    unit: Fast unit tests (run on every push)
    integration: Slow integration tests (run on demand)
    concurrency: Multi-process concurrency tests (run on demand)